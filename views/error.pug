extends ./includes/layout.pug

block content
    .error
        h1 Error N° #{error.status}:
        h2 error.statusText
         a.btn.btn-secondary.show(href="/") Inicio
        .item
            pre #{error.stack}
            p Es despliegue de errores
        img(src="indentation-war.png")